<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("./partials/head") %>
    <title>はつおんテスト</title>
    <script src="https://unpkg.com/wanakana"></script>
  </head>
  <body>
    <%- include('./partials/nav') %>

    <div class="container mt-5 d-flex justify-content-center">
      <div
        class="card mb-4 shadow-sm text-center"
        style="max-width: 500px; width: 100%"
      >
        <p class="fw-bold fs-4 mb-3"><%= targetSentence.japanese %></p>
        <p class="text-muted">Meaning: <%= targetSentence.english %></p>

        <div class="d-flex justify-content-center mb-3">
          <button
            type="button"
            id="startBtn"
            class="btn btn-success btn-lg rounded-circle"
            style="width: 80px; height: 80px; font-size: 1.5rem"
          >
            🎙
          </button>
        </div>

        <div class="d-flex justify-content-center gap-2">
          <button type="button" id="nextBtn" class="btn btn-primary">
            NEXT
          </button>
          <a href="/" class="btn btn-danger">QUIT</a>
        </div>
        <div id="statusMessage" class="mt-2 fs-5"></div>
      </div>
    </div>

    <style>
      .card {
        border-radius: 16px;
        background: #f8f9fa;
        padding: 30px;
        font-size: 1.5rem;
      }
      .btn-primary {
        font-weight: 600;
      }
    </style>

    <%- include('./partials/pronouncetestscript') %> <%-
    include('./partials/script') %>
  </body>
</html>
